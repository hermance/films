import React from "react";

type TypeProps = {|
    i18n: any,
    films:any[]
|};
type TypeState = {||};

export default function  Films (props: TypeProps){

        return (
            <div>
            </div>
        );

}